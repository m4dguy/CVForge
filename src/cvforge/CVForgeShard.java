package cvforge;

/**
 * Interface that needs to be override by Shards.
 * Does not contain any methods, but merely serves as an identifier that CVForge recognizes.
 */
public interface CVForgeShard {}
